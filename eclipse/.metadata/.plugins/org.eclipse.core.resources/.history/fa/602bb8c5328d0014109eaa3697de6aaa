package com.miner3389.nerdspeaq.config;

import java.io.File;

import net.minecraftforge.common.config.Configuration;

public class ConfigurationHandler {
	
	public static Configuration config;
	
	public static void init(File configFile){
		//create config object from file
		//use minalean's tutorial to make a gui!!
		config = new Configuration(configFile);
		
		boolean configValue = true;
		
		try{
			//load the config file
			config.load();
			
			//read in properties from config file
			//do not use ids!!!
			configValue = config.get(Configuration.CATEGORY_GENERAL, "config value", true, "This is simply a test").getBoolean();
		}catch(Exception e){
			//Log exception
			
		}finally{
			//Save config file
			config.save();
			
		}
		
	}
}
